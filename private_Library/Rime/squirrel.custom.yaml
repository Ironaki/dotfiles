# file：~/Library/Rime/squirrel.custom.yaml

patch:
#  us_keyboard_layout: true                 # 键盘选项：应用美式键盘布局
# 状态通知，适当，也可设为全开（always）全关（never）
#  show_notifications_when: appropriate
  style/color_scheme: purity_of_essence  # Theme in squirrel.yaml
#  style/color_scheme: google
  style/horizontal: false                   # 候选窗横向显示
#  style/inline_preedit: false              # 关闭内嵌编码，这样就可以显示首行的拼音
  style/corner_radius: 4                  # 窗口圆角半径
  style/border_height: 8                   # 窗口边界高度，大于圆角半径才有效果
  style/border_width: 12                    # 窗口边界宽度，大于圆角半径才有效果
  style/line_spacing: 5                    # 候选词的行间距
#  style/spacing: 5                         # 在非内嵌编码模式下，预编辑和候选词之间的间距
  style/font_face: "STSong Regular"   # 字体名称
  style/font_point: 19
  preset_color_schemes/purity_of_essence/hilited_candidate_back_color: 0x606060  # Customize preset_theme
